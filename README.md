## Neovim configuration

build from [dotfiles](https://github.com/hendrikmi/dotfiles)

## Setup

mv ~/.config/nvim ~/.config/nvim.bak

rm -rf ~/.local/share/nvim/
rm -rf ~/.local/state/nvim
rm -rf ~/.cache/nvim

git clone git@github.com:chenyukang1/nvim-configuration.git ~/.config/nvim
